import styles from './styles.module.css';

const CreditCard = () => {
    return (
        <div className={`${styles.creditCard} ${styles.creditCardUnfilled}`}>
        </div>
    );
};
export default CreditCard;